<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="CustomSearchView" parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">@color/blue0</item> <!-- 검색어 텍스트 색상 -->
        <item name="android:textColorPrimary">@color/white</item> <!-- 검색어 텍스트 색상 -->
        <item name="android:textColorHint">@color/white</item> <!-- 힌트 색상 -->
        <item name="colorControlNormal">@color/white</item> <!-- 기본 아이콘 색상 -->
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/blue0</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowLightStatusBar" >true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:enforceNavigationBarContrast" >false</item>
        <item name="android:enforceStatusBarContrast" >false</item>
    </style>

</resources>